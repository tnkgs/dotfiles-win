[Settings]
# アニメーションを無効化（ウィンドウ移動の不具合軽減）
gtk-enable-animations=false

# ウィンドウ装飾設定（最小化、最大化、閉じるボタン）
gtk-decoration-layout=:close

# テーマ設定
gtk-theme-name=Adwaita

# ダークモード設定（GTK4 + libadwaita用）
# Note: libadwaitaは color-scheme で制御（アプリケーション側で設定）
